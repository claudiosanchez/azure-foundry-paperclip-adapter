/**
 * UI module exports — used by Paperclip's dashboard for run viewing
 * and agent configuration forms.
 */
export { parseAzureFoundryStdoutLine } from "./parse-stdout.js";
export { buildAzureFoundryConfig } from "./build-config.js";
